<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sep.wed1" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main" />
        <activity android:name=".AddNewGuest" />
        <activity
            android:name=".UpdateGuest"
            android:label="@string/title_activity_update_guest" >
        </activity>
        <activity
            android:name=".MyGuestList"
            android:label="@string/title_activity_my_guest_list" >
        </activity>
        <activity
            android:name=".DeleteGuest"
            android:label="@string/title_activity_delete_guest" >
        </activity>
    </application>

</manifest>
